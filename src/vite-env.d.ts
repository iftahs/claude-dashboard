/// <reference types="vite/client" />

// Injected by Vite `define` (see vite.config.ts) — the package.json version,
// attached to every analytics event as `app_version`.
declare const __APP_VERSION__: string;
