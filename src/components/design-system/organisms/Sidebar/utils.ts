/**
 * The neutral product name: the dashboard shows Claude, Codex or both, so the
 * brand names neither. Shared with the browser tab title (useDocumentTitle);
 * index.html carries the same string for the first paint.
 */
export const BRAND = 'AI Usage';
